import { MostrarColorSeleccionadoPipe } from './mostrar-color-seleccionado.pipe';

describe('MostrarColorSeleccionadoPipe', () => {
  it('create an instance', () => {
    const pipe = new MostrarColorSeleccionadoPipe();
    expect(pipe).toBeTruthy();
  });
});
